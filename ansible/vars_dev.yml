ansible_user: appuser
ansible_hosts: dev_hosts
# CI sets ANSIBLE_PRIVATE_KEY_FILE; locally default to the Beget deploy key.
ansible_ssh_private_key_file: "{{ lookup('env', 'ANSIBLE_PRIVATE_KEY_FILE') | default('~/.ssh/id_beget_ebaluksf', true) }}"

deploy_src: "{{ playbook_dir }}/.."
deploy_dest: /home/appuser/gilmoreplace_2022

compose_file: docker-compose.yaml

rsync_excludes:
  - "--exclude=.git"
  - "--exclude=.venv"
  - "--exclude=node_modules"
  - "--exclude=design"
  - "--exclude=gilmoreplace-next/node_modules"
  - "--exclude=gilmoreplace-next/.next"
  - "--exclude=ansible"
  - "--exclude=media_files"
  - "--exclude=static"
  - "--exclude=prod_server.env"
  - "--exclude=.env.local"
  - "--exclude=.env.production"
  - "--exclude=ansible/logs"
  - "--exclude=__pycache__"
